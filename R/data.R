#' Example of `hyfe_data` object
"hyfe_data"

# Example of a processed `hyfe` object (Hyfe test phones) (all users aggregated)
"ho"

# Example of a processed `hyfe` object (Hyfe test phones) (users processed separately)
"ho_by_user"
